# Runtime dependencies. The authoritative list lives in pyproject.toml -
# prefer `pip install -e .` over installing this file directly.
#
# Optional extras (not listed here):
#   pip install 'PythonAQ[calendar]'  # calendar plot (plotly-calplot, needs pandas<3)
#   pip install 'PythonAQ[app]'       # Streamlit demo application
#   pip install 'PythonAQ[dev]'       # test and lint tooling

numpy>=1.24
pandas>=2.0
plotly>=5.15
scipy>=1.10
requests>=2.28
rdata>=0.11
scikit-learn>=1.3
statsmodels>=0.14
pymannkendall>=1.4
pygam>=0.9
